export default function KitsPage() {
  return (
    <div className="mx-auto max-w-3xl p-8">
      <h1 className="text-2xl font-bold">Your Kits</h1>
      <p className="mt-2 text-zinc-600">No kits yet — create one from the builder (Phase 1).</p>
      <div className="mt-6 rounded border border-dashed p-8 text-center text-zinc-500">Empty state (scored in human review)</div>
    </div>
  );
}
