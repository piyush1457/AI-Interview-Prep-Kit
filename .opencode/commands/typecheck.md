---
description: Type-check the whole project with tsc --noEmit
agent: build
---

Run the TypeScript type-checker (no emit):

```bash
npm run typecheck
```

Fix all type errors found. Report remaining issues if any cannot be resolved.
